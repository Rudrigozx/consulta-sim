<%= form_with(model: consultum) do |form| %>
  <% if consultum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(consultum.errors.count, "error") %> prohibited this consultum from being saved:</h2>

      <ul>
        <% consultum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :medico, style: "display: block" %>
    <%= form.collection_select(:Medico_id, Medico.all, :id, :nome , include_blank: "Selecione um Medico") %>
  </div>

  <div>
    <%= form.label :paciente, style: "display: block" %>
    <%= form.collection_select(:Paciente_id, Paciente.all, :id, :nome, include_blank: "Selecione um paciente") %>
  </div>

  <div>
    <%= form.label :data_hora, style: "display: block" %>
    <%= form.datetime_field :data_hora %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
